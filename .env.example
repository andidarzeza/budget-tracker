# Copy to `.env` next to docker-compose.yml for compose variable substitution (ports below).
# The backend container does NOT load this file — Mongo and profile are set in docker-compose.yml
# so empty SPRING_DATA_* lines here cannot break Spring.

HTTP_PORT=4001
BACKEND_PORT=9000
