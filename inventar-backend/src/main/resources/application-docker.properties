# Used when SPRING_PROFILES_ACTIVE=docker (see docker-compose.yml).
# Duplicates compose SPRING_DATA_MONGODB_URI so Mongo works even if only the profile is set.
spring.data.mongodb.uri=mongodb://mongo:27017/inventar?connectTimeoutMS=10000&serverSelectionTimeoutMS=10000&socketTimeoutMS=30000
